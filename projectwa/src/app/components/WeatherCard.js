"use client";
import React from "react";

const WeatherCard = ({ weather, city }) => {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center p-6 bg-gradient-to-b from-blue-100 to-blue-300"></div>
  );
};
